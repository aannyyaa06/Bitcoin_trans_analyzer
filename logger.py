#  code TO set UP logging in Python. It creates a logger that prints messages to the console with timestamps and log levels (like INFO or DEBUG). This helps track and debug what the script is doing while it runs.
